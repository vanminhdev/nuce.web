@model List<nuce.web.quanly.Models.ExamQuestions>
@{
    ViewData["Title"] = "Đề khảo sát";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row mb-3">
        <div class="col-md-9">
        </div>
        <div class="col-md-3">
            <a href="/survey/createexamquestions" class="btn update-btn text-light w-100 m-2">Thêm đề khảo sát</a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <table id="id-datatable" class="table table-bordered">
                <thead>
                    <tr>
                        <th class="text-center" width="100px">STT</th>
                        <th class="text-center" width="100px">Mã</th>
                        <th class="text-center">Tên</th>
                        <th class="text-center"></th>
                    </tr>
                </thead>
                <tbody>
                    @for(int i = 0; i < Model.Count; i++)
                    {
                        <tr>
                            <td>@(i + 1)</td>
                            <td>@Model[i].code</td>
                            <td>@Model[i].name</td>
                            <td>
                                <a href="/survey/examstructure?examQuestionId=@Model[i].id">Cấu trúc đề</a> |
                                <a href="/survey/examdetail?examQuestionId=@Model[i].id">Nội dung đề</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

